// Global object
// Inside a browser, global object is window. To use the funciton of window just need the function name, no need to use window.xxx

// console.log(global);

// setTimeout(() => {
//     console.log("in the timeout");
//     clearInterval(int) // stop the interval after 3s
// }, 3000); // run once after 3 secs

// const int = setInterval(() => {
//     console.log("in the interval");
// }, 1000); // function run every 1 sec

console.log(__dirname); // the dirctory name
console.log(__filename); // the file name of this js file

